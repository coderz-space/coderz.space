# Coderz Web

Next.js web application for the Coderz platform.

## Quick Start

**Development:**
```bash
npm run dev
```

**Docker:**
```bash
docker build -t coderz-web . && docker run -p 3000:3000 coderz-web
```

App runs at [http://localhost:3000](http://localhost:3000)
